// In which scenario is it impossible to embed data within a document
// (you must put the data in it a separate collection)?

// The embedded data could exceed the 16MB document limit within MongoDB.
